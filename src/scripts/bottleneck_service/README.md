Bottleneck service scripts:

- `run_bottleneck_service.sh`: run the bottleneck-service targeting cases in this folder.
- `cases/`: service-focused cases for local, chain, and fanout bottlenecks.

Example:

```bash
bash src/scripts/bottleneck_service/run_bottleneck_service.sh all observation
```
